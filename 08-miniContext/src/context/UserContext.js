import React from 'react'

// React.creat context kar ka conxt karna ha usa phir 1 user context ma store kar lana ha or bad ma export kr dana ha

const UserContext = React.createContext()

export default UserContext;

// yaha pr ham na provider bna diya ha user context ka is ko sb ka excess mil jaya ga or data ka excess userContext bi la sakta ha

{/* <UserContext>
    <login></login> 
    <cards>
        <data/>
    </cards>
</UserContext> */}
